services:
  blog_app:
    image: "php:8.2-apache"
    ports: 
      - "8000:80"
    volumes:
      - ./app:/var/www/html
      - ./apache/000-default.conf:/etc/apache2/sites-available/000-default.conf
    restart: always
  # blog_db:
  #   image: ""